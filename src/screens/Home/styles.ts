import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  layoutContainer: {
    paddingTop: 30 * 0.35,
    paddingHorizontal: 30,
  },
  categoryContainer: {
    marginEnd: 5,
  },
  listContainer: {
    marginTop: 20,
    gap: 20,
  },
});
