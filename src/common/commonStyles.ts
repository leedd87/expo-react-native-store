import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  layoutContainer: {
    paddingTop: 30 * 0.35,
    paddingHorizontal: 30,
  },
});
