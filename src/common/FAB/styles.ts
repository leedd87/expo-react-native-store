import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  btnStyle: {
    shadowColor: 'black',
    shadowOffset: {
      width: 0,
      height: 10,
    },
    shadowOpacity: 0.4,
    shadowRadius: 10,
    elevation: 3,
    borderRadius: 13,
  },
});
